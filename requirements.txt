PyMuPDF

langchain
langchain-core
langchain-huggingface
langchain-chroma

# Optional tracing for the IT "Pipeline test" diagnostic tool
# (app/routers/it.py) only — inert unless LANGSMITH_API_KEY is set.
langsmith

chromadb
sentence-transformers
rank_bm25

# Web application (multi-tenant SaaS layer)
fastapi
uvicorn
python-multipart
email-validator
python-dotenv

# Database, auth, and storage. Schema + RLS policies live in
# supabase/migrations/, applied directly against the project — not by this
# app at startup. sqlalchemy/alembic/python-jose/bcrypt are gone: Supabase
# Auth issues and verifies tokens, and RLS is what enforces tenancy, not
# an ORM query filter.
supabase
psycopg2-binary

# LLM provider. Only the one named by LLM_PROVIDER is imported at runtime
# (the imports live inside each provider's complete()), so the others can
# be dropped without breaking the app. openai's SDK also backs the
# "openrouter" provider (OpenRouter is OpenAI-compatible), not just "openai".
groq
openai

# Development / testing
pytest
httpx
